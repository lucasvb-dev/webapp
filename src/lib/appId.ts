/** Change when forking this template (storage keys, IndexedDB, custom events). */
export const APP_ID = "my-app";

export const STORAGE_PREFIX = `${APP_ID}:`;

export const ROUTE_CHANGE_EVENT = `${APP_ID}:routechange`;
